function splitSubtier(subtier) {
    var probs = [1, 0];
    var lasts = [0];
    for (let i = 1; i < len(subtier); i++) {
      console.log(subtier[i]);
    }
  }

  splitSubtier("updateMemberCreditStatusForPayNow");